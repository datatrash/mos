[workspace]
members = [
    "mos",
    "mos-core",
    "mos-testing"
]

[profile.dev]
split-debuginfo = "unpacked"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
